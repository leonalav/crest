model: debug
batch_size: 512
max_steps: 600
tbptt_k: 8
learning_rate: 0.0003
min_learning_rate: 0.00003
warmup_steps: 100
weight_decay: 0.1
grad_clip_norm: 1.0
gate_regularization_weight: 0.0
gate_target: 0.5
precision: fp16
seed: 1337
num_workers: 2
log_every: 50
eval_every: 100
save_every: 600
